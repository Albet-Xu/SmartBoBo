/**
 * Host loader entry for the browser implementation exported from `./client`.
 * @module @deepseek-ai/dsh-client-ui-settings-proxy/index
 */

/** Host plugin body — no host-side behavior for the proxy settings domain. */
export function apply(): void {}
