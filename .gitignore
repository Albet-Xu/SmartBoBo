# ── 环境与依赖（不提交）─────────────────────────────────────────────────
# 密钥 / 环境变量（含真实 API Key，绝不入库；.env.example 保留为模板）
.env
.env.*
!.env.example

# Node 依赖
node_modules/

# Python 虚拟环境（uv 管理，位于项目根 .venv；由 uv sync 生成，不应提交）
.venv/
venv/

# 用户级 dsh 数据（位于 HOME 下，本就不在工作区内；此处显式声明以防混淆）
~/.dsh/

# ── Python 缓存 ────────────────────────────────────────────────────────
__pycache__/
*.py[cod]

# ── 采集结果（本地数据，不入库）────────────────────────────────────────
# data/ 下是采集到的网页原始数据，全部不入库；仅保留目录占位 .gitkeep
data/*
!data/.gitkeep

# ── DBX 数据库运行时：二进制 / 前端dist / 数据库数据 入库（居家拉取即用）；仅运行日志不入库 ──
dbx-runtime/*.log

# ── 临时文件/文档 ──────────────────────────────────────────────────────
*.tmp

# ── 发布/打包产物与个人文档（不入库）────────────────
release/
dsh/msn_baxian_article.md
