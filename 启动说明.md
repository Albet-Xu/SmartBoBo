# BoBo 智能采集平台 · 启动与使用说明

基于 **DeepSeek Harness（dsh）+ 采集引擎** 的本地智能采集平台。

你只需要在浏览器里和智能体聊天，告诉它要采集的网页地址，它就会自动用真实浏览器访问该页面，并把页面**完整的 HTML 原始数据保存到本地**。

> **首次部署到新机器**：请先看同目录《部署说明.md》（含环境安装、依赖、密钥配置、启动）。本文件是**本机已配置好之后**的日常启动与使用说明。下文中 `<项目根>` 指 BoBo 项目所在目录（即本项目文件所在位置）。

---

## 一、项目结构

```
<项目根>/                                # 即 BoBo 项目所在目录
├── dsh/                     # DeepSeek Harness 源码（基于 pnpm workspace 的 monorepo）
│   ├── package.json         # 根配置；含 bobo 启动脚本
│   ├── packages/
│   │   ├── acquisition/tool-acquisition/   # 【采集工具插件】crawl_fetch 的实现
│   │   └── bobo/cordis.patch.yml           # 端口补丁（固定 7070）
│   └── patch/web-acquisition.yml           # 装配补丁（把采集插件注入 Web）
├── scripts/                 # Python 采集脚本（真正抓网页的那一步）
│   ├── run_camoufox.py      # 默认引擎：camoufox（抗检测浏览器）
│   ├── run_scrapling.py     # 引擎 2：Scrapling
│   └── run_crawl4ai.py      # 引擎 3：Crawl4AI
├── engines/                 # 采集引擎源码（scrapling / crawl4ai / camoufox）
├── .venv/                   # Python 虚拟环境（由 uv 管理，内置上述引擎）
├── pyproject.toml           # Python 环境定义（依赖：引擎本地路径 + camoufox；uv 使用）
├── uv.lock                  # Python 依赖锁定（uv 自动生成，交付给 uv.sync 复现环境）
├── .python-version          # 解释器版本固定（3.13）
├── data/                    # ★ 采集结果输出目录（所有 .html 原始数据落在这里）
│   ├── HTML/                # （可选）你自定义的分类目录
│   └── TXT/
├── docs/                    # 说明文档
├── 部署说明.md              # 全新机器部署指引（见《一》提示）
├── 启动说明.md              # 本文件（日常启动与使用）
├── 启动.cmd                 # Windows 一键启动脚本（自动设置 BOBO_ROOT）
└── 启动.sh                  # Linux / macOS 一键启动脚本
```

---

## 二、环境要求（本机已配置好；新机器见《部署说明.md》）

> 下表是**本机当前**的环境状态。若在全新机器上，请按《部署说明.md》《三~四》准备依赖 + `.env` 密钥。

| 组件 | 版本 | 状态 |
| --- | --- | --- |
| Node.js | v24 | ✅ 已装 |
| pnpm | 11.7 | ✅ 已装 |
| Python | 3.13 | ✅ 已装（`.python-version` 固定） |
| uv | 0.11+ | ✅ 已装（Python 环境管理器） |
| dsh 依赖 + 构建 | — | ✅ 已 `pnpm install` + `pnpm run build` |
| Python 引擎 | scrapling / crawl4ai / camoufox | ✅ 已装入 `.venv`（uv 管理） |
| camoufox 浏览器内核 | v152 | ✅ 已下载 |

> **关于 Python 环境**：本项目用 **uv** 管理 Python 依赖，环境统一放在项目根 `.venv/`。
> - 环境定义：`pyproject.toml`（依赖：`engines/scrapling`、`engines/crawl4ai` 本地路径 + `camoufox`）。
> - 一键同步/重建：`uv sync`（按 `uv.lock` 复现；首次或换机器时在 `BoBo/` 根目录执行）。
> - 单独跑脚本：`uv run python scripts/run_xxx.py ...`（自动使用 `.venv`）或 `.venv/Scripts/python.exe ...`。
> - 以上确认过，无需你重装。重装场景见文末《附录 B》。

---

## 三、唯一要确认的事：API Key（已配置）

dsh 靠 **DeepSeek API Key** 驱动智能体对话。配置文件在 `<项目根>/dsh/.env`（即 `dsh/.env`）：

```env
DEEPSEEK_API_KEY=sk-你的Key
```

> 已确认 `.env` 里已填入真实 Key。若 Key 失效/更换，直接编辑该文件改 Key 后重启即可。`.env` 已被 gitignore，不会进版本库。

---

## 四、启动项目

### 4.1 启动（推荐：启动脚本）

回到项目根目录 `<项目根>/`：

- **Windows**：双击或运行 `启动.cmd`
- **Linux / macOS**：运行 `./启动.sh`（首次先 `chmod +x 启动.sh`）

脚本会自动把 `BOBO_ROOT` 指向本机项目根目录（采集插件据它定位 `.venv` 与 `scripts`），无需手动改任何路径。

### 4.2 启动（手动方式，可选）

在终端执行：

```bash
cd <项目根>/dsh
pnpm bobo
```

> - `packages/bobo/cordis.patch.yml` —— 把 Web 端口固定为 **7070**；
> - `patch/web-acquisition.yml` —— 把采集插件 `crawl_fetch`（`@deepseek-ai/dsh-tool-acquisition`）注入装配。

看到 `dsh web: http://127.0.0.1:7070` 即启动成功。

### 4.3 打开界面

浏览器访问：

```
http://127.0.0.1:7070
```

---

## 五、使用：让智能体采集网页

在对话框里用自然语言告诉智能体要采集的网址，例如：

> **“请采集 https://zhuanlan.zhihu.com/p/xxx 这个网页。”**

或

> **“用采集工具抓取 https://example.com。”**

智能体会调用 `crawl_fetch` 工具 → 通过 camoufox（默认）真实浏览器渲染页面 → 把整页 HTML 保存到本地，并回你一句类似：

```
采集完成:
- 已保存: data/站点_标题_时间戳.html
- HTTP状态: 1
- 预览: …（正文开头一小段）
```

---

## 六、采集结果存哪里（★重点）

所有采集到的**原始数据就是网页的完整 HTML**，默认存到项目根目录的 `data/`：

```
<项目根>/data/
```

文件名自动生成，形如：

```
zhuanlan_zhihu_com_文章标题_20260819-131933.html
```

- 命名规则：`站点域名_页面标题_时间戳.html`
- **只保存 .html，不生成 .json**（原始数据即 HTML）
- 这些 `.html` 就是你要的网页完整内容，可任意用浏览器/编辑器打开查看或归档

> 小提示：如果想让 AI 返回完整 HTML 文本（而不只是保存），当前设计是“AI 不读取 HTML、只负责把 HTML 落盘并返回路径/预览”，这是你确定的方向。

---

## 七、常见操作

| 你想做什么 | 怎么做 |
| --- | --- |
| 重启服务 | 运行启动脚本（`启动.cmd` / `./启动.sh`），或 `cd <项目根>/dsh && pnpm bobo` |
| 停止服务 | 在运行 bobo 的终端按 `Ctrl+C` |
| 换采集引擎 | 对话里说 engine，如“用 scrapling 采”，或改插件默认值（见附录 A） |
| 改 Python 采集脚本 | 编辑 `scripts/*.py` 后**重启** bobo 生效（或单独测试脚本） |
| 改插件 TS 代码 | `packages/acquisition/tool-acquisition/src/index.ts` → 编译 → 重启 |
| 清空采集数据 | 删除 `data/` 下不需要的 `.html` |

---

## 八、重新编译插件（改了 TS 代码后）

```bash
cd <项目根>/dsh
pnpm run build:lib:host        # 只重编译 host 面（含采集插件），较快
# 或全量： pnpm run build
pnpm bobo                      # 重启生效
```

---

## 附录 A：如何切换/新增采集引擎

当前三个 Python 脚本：`run_camoufox.py`（默认）、`run_scrapling.py`、`run_crawl4ai.py`。

- 对话指定：对智能体说 engine，例如“用 scrapling 采集 https://…”。
- 改默认：编辑 `packages/acquisition/tool-acquisition/src/index.ts` 里 `const engine = args.engine ?? 'camoufox'` 的 `'camoufox'` 为其它引擎名，然后重新编译 + 重启。

新增引擎：在 `scripts/` 加 `run_<name>.py`（参考现有脚本，接收 `--url/--out/--auto-name` 并输出单行 JSON 到 stdout），再把引擎名加进插件与说明即可。

---

## 附录 B：环境重建（仅当换机器/重装时）

> 完整步骤见同目录《部署说明.md》。此处是精简版（路径以 `<项目根>` 指代 BoBo 所在目录）：

```bash
# 1) Node 依赖 + 构建
cd <项目根>/dsh
pnpm install
pnpm run build

# 2) 配置 Key
# 编辑 <项目根>/dsh/.env 填入 DEEPSEEK_API_KEY

# 3) Python 引擎（用 uv 管理环境，位置为 <项目根>/.venv）
cd <项目根>
uv sync                     # 按 pyproject.toml + uv.lock 一键创建 .venv 并装好全部依赖
uv run camoufox fetch       # 下载 camoufox 浏览器内核（首次使用才需要）

# 4) 启动
# Windows:  <项目根>/启动.cmd    Linux/macOS: <项目根>/启动.sh
cd <项目根>/dsh && pnpm bobo
```

---

## 故障排查

| 现象 | 处理 |
| --- | --- |
| 智能体不回复 | 检查 `dsh/.env` 的 Key；重启 bobo |
| 采集报脚本错误 | 单独测：`cd <项目根> && uv run python scripts/run_camoufox.py --url https://example.com --out data/t --auto-name` |
| 某些网站取不到正文 | 多半是页面强反爬/需登录/纯 JS。camoufox 已做渲染+弹窗处理，仍不行可换爬虫或给登录态（后续可扩展） |
| 端口占用 | 关掉其它占 7070 的进程，或改 `packages/bobo/cordis.patch.yml` 里的 `port` |
```

> 更详细的技术说明见 `docs/02-操作指南-修订版-v2.md`。
