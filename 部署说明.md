# BoBo 智能采集平台 · 部署说明

> 面向**全新环境的一次性搭建**。本文档把项目部署到任意一台新电脑（Windows / Linux / macOS），
> 使其能运行本地智能采集服务。日常使用与操作见同目录 `启动说明.md`。

BoBo 是一个基于 **DeepSeek Harness（dsh）+ Python 采集引擎**的本地智能采集平台：
在浏览器里和智能体聊天，告诉它网页地址，它就用真实浏览器渲染页面、把整页内容保存到本地。
项目已做**可移植化处理**：不再写死任何机器绝对路径，任何路径下 `git clone` 均可直接部署运行。

---

## 一、系统要求

| 组件 | 版本要求 | 说明 |
| --- | --- | --- |
| Node.js | `^22.19.0` 或 `>=24.0.0` | dsh 要求；推荐安装 LTS 24 |
| pnpm | `11.x`（项目用 `11.7.0`） | 通过 `corepack enable` / `npm i -g pnpm` 安装 |
| Python | `3.10+`（项目锁 `3.13`） | 由 uv 自动按 `.python-version` 安装 |
| uv | `0.11+` | Python 环境管理器（[安装教程](https://docs.astral.sh/uv/)） |

> 平台差异：Windows 用 `启动.cmd`，Linux/macOS 用 `启动.sh`，其余命令两平台通用。

---

## 二、获取代码

把项目克隆（或拷贝）到你想要的任意路径，**不要在路径里写死**：

```bash
git clone <你的仓库地址> BoBo
cd BoBo
```

> 项目结构：`dsh/`（DeepSeek Harness 后端）、`scripts/`（Python 采集脚本）、
> `engines/`（采集引擎源码）、`data/`（采集结果落盘目录）。详见 `启动说明.md`。

---

## 三、配置 API Key（必做）

dsh 靠 **DeepSeek API Key** 驱动智能体。配置文件是 `dsh/.env`（该文件已 gitignore，不会进版本库，请勿提交）。

```bash
# 首次：用模板复制出 .env（Windows 用 copy 命令）
cp dsh/.env.example dsh/.env     # Linux/macOS
copy dsh\.env.example dsh\.env   # Windows

# 编辑 dsh/.env，填入你的真实 Key：
#   DEEPSEEK_API_KEY=sk-你的Key
```

---

## 四、安装依赖

### 4.1 Node 侧（dsh 后端）

```bash
cd dsh
pnpm install        # 按 pnpm-lock.yaml 安装全部依赖
pnpm run build      # 编译（生成各包的 lib/ 产物，含采集插件 tool-acquisition）
cd ..
```

> 采集插件 `@deepseek-ai/dsh-tool-acquisition` 已声明在 `apps/cli` 依赖里，
> 启动时 dsh 会自动把它挂载到本地 profile，因此**无需手动配置任何绝对路径**。

### 4.2 Python 侧（采集引擎）

在项目根目录（`BoBo/`）执行：

```bash
uv sync                # 按 pyproject.toml + uv.lock 创建 .venv 并装好全部引擎
uv run camoufox fetch  # 下载 camoufox 浏览器内核（首次必须；仅需一次）
```

- 依赖（scrapling / crawl4ai / camoufox / html2text）全部由 `uv.lock` 锁定，换机器可完整复现。
- 若某台机器已有 Python 3.13，uv 会自动复用；否则自动下载。

---

## 五、启动

回到项目根目录，运行启动脚本（脚本会自动把 `BOBO_ROOT` 指向本机项目路径，无需手动改任何配置）：

**Windows：**
```bat
启动.cmd
```

**Linux / macOS：**
```bash
chmod +x 启动.sh   # 首次授予执行权限
./启动.sh
```

看到 `dsh web: http://127.0.0.1:7070` 即启动成功。浏览器打开：

```
http://127.0.0.1:7070
```

> 等价手动方式：`cd dsh && pnpm bobo`（此时需自行保证 `BOBO_ROOT` 已指向 BoBo 根目录，
> 或让采集插件回退到默认值 `python`/相对路径）。

---

## 六、验证是否部署成功

1. 浏览器打开 `http://127.0.0.1:7070`，能看到对话界面。
2. 给智能体发：
   > “请采集 https://example.com 这个网页。”
3. 完成后应回一条：已保存的路径 + HTTP 状态码 + 预览。采集到的 `.html`/`.md` 会落在项目根 `data/` 目录。

---

## 七、常见问题

| 现象 | 处理 |
| --- | --- |
| 智能体不回复 | 检查 `dsh/.env` 的 `DEEPSEEK_API_KEY`；重启服务 |
| `pnpm` 找不到 | 先 `npm i -g pnpm` 或启用 corepack |
| 采集报脚本错误 | 单独测：
  `uv run python scripts/run_camoufox.py --url https://example.com --out data/t --auto-name` |
| 端口 7070 被占用 | 关掉占用进程，或改 `dsh/packages/bobo/cordis.patch.yml` 的 `port` |
| Linux 下启动无权限 | `chmod +x 启动.sh` |
| camoufox 报 manifest 缺失 | 确认执行过 `uv run camoufox fetch` |
| 某些网站取不到正文 | 页面强反爬/需登录/纯 JS；可让智能体换引擎（scrapling / crawl4ai） |

---

## 八、重置 / 迁移到另一台机器

1. 新机器重复本说明第二节到第五节。
2. `dsh/.env` 的 Key 每台机器各自配置（不入库）。
3. 采集数据在 `data/`，属本地文件不入库；需要迁移就单独拷贝。
